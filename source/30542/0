let factorial n = 
    let rec fac acc n =
        if n > 0 then
            fac (acc * n) (n - 1)
        else
            acc

    fac 1 n

let summary n = 
    let rec sum acc n =
        if n > 0 then
            fac (acc + n) (n - 1)
        else
            acc

    sum 1 n