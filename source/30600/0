// With the latest versions Slices can be used to split a list into two parts.  
// This split retains the order of the elements of the list but it doesn't handle an empty list gracefully
let split5 (s :'a List)  =
    let sl = s.Length/2
    (s.[..sl], s.[sl+1..]) 