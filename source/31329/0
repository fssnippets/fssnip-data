open System

[<EntryPoint>]
let main argv =
    printf "Password? "
    let hint = Console.ReadLine()
    if hint = "/s/zZQ7" then
        printfn "Richtig."
        0
    else
        printfn "Falsch."
        1